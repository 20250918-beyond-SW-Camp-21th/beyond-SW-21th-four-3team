spring:
  mail:
    host: smtp.gmail.com
    port: 587
  application:
    name: todo-dodo
  profiles:
    active: local

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      show-sql: false

server:
  port: 8080

logging:
  level:
    root: info